// Operadores Lógicos: NOT(!), AND(&&), OR(||)
//Operador NOT(!) = Inverter o valor lógico.

let ligado = true;
let desligado = !ligado;

/* 
    Tabela (NOT)
    !true = false
    !false = true
*/

let contratado = aprovado && sabeIngles;

